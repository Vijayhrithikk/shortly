root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/worker.exe ./cmd/worker"
bin = "tmp/worker.exe"
full_bin = "tmp/worker.exe"
include_ext = ["go"]
exclude_dir = ["tmp", "vendor"]
delay = 1000

[log]
time = true